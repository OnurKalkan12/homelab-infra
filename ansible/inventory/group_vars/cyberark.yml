# Variables specific to CyberArk Windows hosts.
# ansible_password is set per-host in hosts.yml using ansible-vault encryption.

# WinRM connection defaults (shared by all CyberArk hosts)
ansible_connection: winrm
ansible_winrm_transport: basic
ansible_port: 5985
ansible_winrm_server_cert_validation: ignore
